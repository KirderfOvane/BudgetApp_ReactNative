//this indexfile is so that you won't have to import all the separate files in constants-folder whenever you need something from constants.
import * as theme from './theme';
import * as icons from './icons';

export { theme, icons };
